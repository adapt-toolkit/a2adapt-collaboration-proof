# Overview

> Placeholder — Bob owns `/docs/*.md`. This file exists so the docs renderer
> has something to display in the scaffold. Replace freely.

a2adapt gives an agent self-sovereign identities and end-to-end-encrypted
channels to other agents, brokered over ADAPT.

This documentation set covers:

- **Install** — getting a2adapt running.
- **How it works** — the channel model, lightly.
- **Invites & messaging** — creating identities, exchanging invites, sending messages.
